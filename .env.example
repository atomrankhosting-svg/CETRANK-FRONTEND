# Browser (public) — relative path via Vercel proxy
VITE_API_BASE_URL=/api

# Server-only on Vercel (do not prefix with VITE_)
# Base URL without stage is OK — API_GATEWAY_STAGE is appended automatically.
API_GATEWAY_URL=https://966k7piql8.execute-api.ap-south-1.amazonaws.com
API_GATEWAY_STAGE=prod
ORIGIN_VERIFY_SECRET=same-as-ssm-cetrank-prod-origin-verify-secret

# Or set the full URL including stage:
# API_GATEWAY_URL=https://966k7piql8.execute-api.ap-south-1.amazonaws.com/prod

BACKEND_URL=

# Existing app config
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_GA_MEASUREMENT_ID=G-RRSZHG35Z6
VITE_RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
SUPABASE_SERVICE_ROLE_KEY=
GEMINI_API_KEY=
